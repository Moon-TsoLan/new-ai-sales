## 项目背景
项目名称：基于 AI 的商品属性与销售关联系统设计与实现

项目目标：通过文本与图像多模态特征提取、销售预测建模与 SHAP 可解释性分析，实现“输入商品信息 -> 预测销售指标 -> 可解释展示 -> 历史可追溯”的完整系统流程。

---

## 零、数据预处理流程（论文开头补充）

为保证后续模型训练和系统推理的一致性，项目在训练前先执行统一的数据预处理流程。核心步骤如下：

1. 预处理原始表格 `data/raw/input.xlsx`  
   - 删除无关字段  
   - 将 `discount` 与 `description` 的内容进行互换修正  
   - 删除 `images` 或 `description` 为空的数据  
   - 从 `product_details` 中提取材质字段  
   - 将处理结果保存为 `data/processed/processed.csv`

2. 根据 `images` 字段下载图片并清洗坏样本  
   - 按图片 URL 下载到本地图片目录  
   - 下载失败或图片不可用的样本直接删除，保证图像分支训练可用

3. 构建增强后的训练元数据  
   - 由于原始元数据缺少销量和复购率字段，先通过数据增强方式为每条样本补全 `sales` 与 `repeat_rate`  
   - 将增强后的数据保存到 `data/processed/new_input_processed.csv`

4. 构建部分 BIO 标注数据  
   - 对部分样本调用 Minimax API 进行 BIO 标注  
   - 标注脚本位于 `src/BIO_tag.py`  
   - 打标结果输出到 `output` 目录

5. 生成关联模型训练输入集 `A_final_input.csv`  
   - 在 BERT 与 ResNet 训练完成后，以 `data/processed/new_input_processed.csv` 中的 `pid, description, title, images` 作为输入  
   - 执行文本特征与图像特征提取，并与原样本信息合并  
   - 将提取后的融合特征保存到 `data/processed/A_final_input.csv`  
   - 该文件作为回归模型与决策树模型训练的统一输入

---

## 一、模型训练与数据集构建

### 1. 文本特征提取模型
1. 数据预处理（`input.csv`）  
   - 去除无关字段：`_id, crawled_at, out_of_stock, seller, url`  
   - 修改原数据集的列名：将discount和description交换
   - 去除 `images` 或 `description` 为空样本  
   - 从 `product_details` 中抽取材质字段  
   - 形成结构化参数 `{类型, 次级类型, 材质, 颜色, 品牌}`  
   - 保存 `title, description, parameters` 到 `data/processed/processed.csv`
2. 模型训练  
   - 基于 `bert-base-uncased` 训练分类模型与序列标注模型  
   - 分类模型：提取 `category, sub_category`  
   - 序列标注模型：提取 `fabric, color, brand`
3. 模型产物  
   - `model/bert_classifier/best_classifier.pth`  
   - `model/bert_ner/best_ner_model.pth`

### 2. 图像风格与主色模型
1. 数据预处理（`new_input.csv`）  
   - 下载图片到 `data/images`，命名 `pid.jpg`  
   - 清理无关字段  
   - 保存到 `data/processed/new_input_processed.csv`
2. 特征与聚类  
   - 基于 ResNet50 提取图像向量  
   - KMeans 聚类得到风格簇 `style`  
   - 基于规则提取主色 `main_color`
3. 数据产物  
   - `data/processed/new_input_style_color.csv`  
   - 聚类索引模型：`model/resnet50/A_style_kmeans.pkl`

### 3. 关联预测模型
1. 组合特征：`category, sub_category, fabric, color, brand, main_color, style`
2. 目标变量：`sales, repeat_rate, average_rating`
3. 训练模型  
   - 回归模型（多输出）  
   - 决策树模型（多输出）
4. 模型产物  
   - `model/regression_model/best_regression_model.pth`  
   - `model/decision_tree_model/best_decision_tree_model.pth`

### 4. 可解释性验证
1. 使用 SHAP 对测试集进行解释  
2. 输出 summary/bar/waterfall/force 多类型图表  
3. 离线主流程脚本：`src/shap_association_explain.py`

---

## 二、系统现状（当前实际实现）

当前项目已从“离线脚本验证”升级为“全栈可交互系统”，包含前端、后端、数据库、文件存储与历史追溯。

### 1. 当前技术栈（实际落地）
- 前端：React + TypeScript + Vite + Axios + React Router
- 后端：FastAPI + SQLAlchemy + JWT
- 数据库：SQLite（默认开发环境），结构与 PostgreSQL 兼容
- 模型集成：直接集成在 FastAPI 进程中，复用 `src/shap_association_explain.py` 的核心函数
- 文件存储：本地文件系统（`uploads`）
- 运行脚本：`start_all.bat` / `stop_all.bat`

### 2. 当前目录结构（核心）
- `backend/`：API、鉴权、数据库、模型推理服务
- `frontend/`：页面、表单、历史列表、结果展示与图像预览
- `model/`：训练后的模型权重与编码器
- `src/`：离线训练与解释脚本
- `uploads/`：用户上传与 SHAP 结果文件

---

## 三、数据库设计（当前实现）

### 1. 用户表 `users`
- `id`：主键，自增
- `username`：唯一，必填
- `email`：唯一，必填
- `password_hash`：bcrypt 哈希
- `created_at`：创建时间

### 2. 预测记录表 `predictions`
- `id`：主键，自增
- `user_id`：外键，关联 `users.id`
- `product_name`：产品名称
- `product_desc`：产品描述
- `image_path`：上传图片相对路径
- `features_snapshot`：JSON 结构快照（包含提取特征、模型预测值、图表路径映射）
- `predicted_sales`：主展示销量（默认优先回归）
- `repeat_rate`：主展示复购率
- `average_rating`：主展示评分
- `shap_plot_path`：主展示 SHAP 图路径
- `created_at`：创建时间

---

## 四、后端接口实现（当前）

统一返回格式：
`{"code": 0, "msg": "success", "data": ...}`

鉴权方式：
除注册/登录/刷新外，均需 `Authorization: Bearer <access_token>`

### 1. 认证模块
- `POST /api/auth/register`  
  - 入参：`username, email, password`
  - 校验：用户名唯一、邮箱唯一、邮箱必填、密码必填
- `POST /api/auth/login`  
  - 返回：`access_token, refresh_token`
- `POST /api/auth/refresh`  
  - 使用 refresh token 刷新 access token

### 2. 预测模块
- `POST /api/predict`（multipart/form-data）  
  - 入参：`product_name, product_desc, image_file`
  - 流程：
    1) 保存上传图片  
    2) 调用文本/图像特征提取  
    3) 分别调用回归模型与树模型预测  
    4) 生成全套 SHAP 图（回归 + 树，三目标）  
    5) 记录写入数据库并返回结果
- `GET /api/predict/{id}`  
  - 返回指定预测详情

### 3. 历史模块
- `GET /api/history?page=1&page_size=20`  
  - 分页返回当前用户历史，按时间倒序
- `GET /api/history/{id}`  
  - 返回历史详情（含图片与可视化路径）
- `DELETE /api/history/{id}`  
  - 删除数据库记录，同时删除对应提交目录文件

---

## 五、在线模型推理与可解释性流程（当前）

### 1. 入口
- 后端入口：`backend/app/api/routes/predict.py`
- 推理核心：`backend/app/services/ml_service.py`

### 2. 复用离线代码
后端在线推理复用了 `src/shap_association_explain.py` 中以下关键函数：
- `_predict_text_features`
- `_predict_image_features`
- `_predict_with_model`
- `_load_background_features`
- `_save_multi_plots`

### 3. 图表生成策略（当前已实现）
每次预测生成：
- 模型维度：`regression` + `tree`
- 目标维度：`sales`、`repeat_rate`、`average_rating`
- 图表类型：`summary`、`bar`、`waterfall`、`force(html)`

即每次提交可生成完整可解释性资产：
- 18 张 PNG（2 模型 * 3 目标 * 3 图）
- 6 个 force HTML（2 模型 * 3 目标）
- 再加原始上传图

---

## 六、文件存储策略（当前）

### 1. 存储路径
每次提交使用独立目录，格式：
`uploads/{user_id}/{submission_id}/`

目录中保存：
- 用户上传原图
- 全套 SHAP 图片与 HTML

### 2. 数据库存储
数据库中仅保存相对路径，如：
- `image_path = 7/20260402090516308758/20260402090516309759.png`
- `shap_plot_path = 7/20260402090516308758/summary_regression_sales.png`

### 3. 静态访问
FastAPI 挂载静态路由 `/uploads`，前端通过 URL 直接访问文件。

### 4. 删除策略
删除历史记录时按提交目录整目录清理，避免残留文件。

---

## 七、前端实现（当前）

### 1. 页面路由
- `/login`：登录页
- `/register`：注册页
- `/`：主界面（需登录）

### 2. 主界面核心功能
1. 左侧历史记录栏（支持展开/收回动画）
2. 新增会话机制：单次问卷提交后锁定，需“开启新对话”再提交下一次
3. 问卷输入：产品名称、产品描述、图片上传（含缩略图预览）
4. 结果展示：
   - 回归/树模型切换
   - 三目标 SHAP 分组展示
   - 图片点击放大（灯箱预览）
   - force 图链接可打开
5. 历史回显：
   - 点击历史记录可回填真实问卷文本与真实图片
   - 支持新旧记录兼容展示

---

## 八、运行与维护（当前）

### 1. 一键启动
`start_all.bat`
- 自动清理旧端口
- 启动后端（8000）与前端（5173）

### 2. 一键关闭
`stop_all.bat`
- 按端口关闭后端与前端进程

### 3. 本地访问
- 前端：`http://localhost:5173`
- 后端：`http://127.0.0.1:8000`

---

## 九、与早期方案相比的关键变更
1. 从“单图 SHAP 返回”升级为“全套 SHAP 资产输出与前端分模型展示”
2. 从“扁平 uploads 存储”升级为“按用户+提交目录隔离”
3. 注册逻辑增强：邮箱必填且唯一，用户名与邮箱双重去重
4. 前端从基础表单升级为可追溯、可切换、可放大预览的完整业务界面
5. 增加一键启动/关闭脚本，降低部署与答辩演示成本

---

## 十、论文撰写可直接引用的实现要点
1. 采用“离线训练 + 在线推理复用”策略，保证研究代码与工程代码一致性
2. 采用双模型并行解释（回归与树）提升结果可解释对比性
3. 采用按提交目录隔离的文件策略，提升可追踪性与清理可控性
4. 采用 JWT + 历史记录机制，实现用户级数据隔离与结果复现
5. 采用前后端分离架构，支持后续扩展到 PostgreSQL、对象存储与异步任务队列


## 项目背景
项目名称：基于AI的商品属性与销售关联系统设计与实现

项目目标：通过深度学习和机器学习技术，建立商品属性与销售数据之间的量化关联模型，为新品开发和老品优化提供数据驱动的决策支持。

模型训练与背景数据集构建流程：
一、文本特征提取模型的训练
1、对自然语言处理数据集input.csv预处理：
    1.从input.csv中去掉_id,crawled_at,out_of_stock,seller,url字段
    2.从input.csv中去掉images或discount为空的数据
    3.从product_details的子字段中提取材质，作为新的字段
    4.将{类型，次级类型，材质，颜色，品牌}作为新的字段"parameters"
    5.将{产品名称，产品描述，产品参数}保存为csv文件放在./data/processed/processed.csv
2、训练文本特征提取模型：使用bert_base_uncased预训练模型进行训练
    1.训练出bert分类器模型，用于从{产品名称，产品描述}中提取{类型，次级类型}，模型存储于./model/bert_classifier/best_classifier.pth
    2.训练出bert序列抽取模型，用于从{产品名称，产品描述}中提取{材质，颜色，品牌}，模型存储于./model/bert_ner/best_ner_model.pth

二、图像特征提取模型的训练
1、对图像特征提取模型数据集new_input.csv预处理：
    1.下载图片至data/images目录，图片名称为pid.jpg
    2.从new_input.csv中去掉_id,crawled_at,out_of_stock,seller,url等字段
    3.将预处理后的数据集保存至data/processed/new_input_processed.csv
2、训练图像风格和主颜色提取模型：使用ResNet50预训练模型和KMeans聚类算法
    1.通过将图片放入ResNet50模型中前向传播，提取图片特征
    2.使用KMeans聚类算法将图片特征聚类为n个风格簇(n取在当前数据集上最优的k值)
    3.将风格簇和主颜色保存至data/processed/new_input_style_color.csv
    4.保存当前n个风格簇的索引，用于后续复用

三、关联与预测模型的构建
1、文本特征提取
    1.从data/processed/new_input_processed.csv中读取{pid，title，description}，命名为data
    2.将{title，description}作为bert_classifier的输入，提取{category，sub_category}，并合并入data
    3.将{title，description}作为bert_ner的输入，提取{fabric，color，brand}，并合并入data
2、图像特征提取
    1.根据data中的pid，找到images目录下的图片，将图片放入ResNet50模型中前向传播，提取代表主颜色和风格的多维向量
    2.将多维向量按照主颜色划分规则和KMeans索引，分配{main_color,style}的值，并合并入data

3、构建两种关联模型：回归分析 + 决策树
    1.将data划分为训练集和测试集，训练集为随机的90%数据，测试集为剩余的10%数据
    2.使用训练集中的{category，sub_category，fabric，color，brand，style，color}作为自变量，{sales，repeat_rate，avarage_rating}作为因变量，构建回归分析模型和决策树模型
    3.将回归分析模型和决策树模型保存至model/regression_model/best_regression_model.pth和model/decision_tree_model/best_decision_tree_model.pth

4、关联模型测试与可解释性分析
    1.将测试集中的{category，sub_category，fabric，color，brand，style，color}作为自变量，输入回归分析模型和决策树模型
    2.将得到的{sales，repeat_rate，avarage_rating}同元数据做对比，计算差值
    3.使用SHAP进行可解释性分析，并将分析结果以多元的可视化展示

技术栈：Python, PyTorch, TensorFlow/Keras, Scikit-learn, Matplotlib/Seaborn/Plotly


全栈开发流程：
一、项目概述

1.用户提交产品名称、产品描述、产品图片。
2.后端调用已有的特征提取代码（文本+图像），提取特征向量。
3.将特征输入预训练好的回归模型，预测销售信息（例如预估销量、销售额等）。
4.基于模型和本次预测的特征，生成 SHAP 可解释性可视化图表（如特征重要性图）。
5.用户需登录，并可查看所有历史预测记录。
用户需登录，并可查看所有历史预测记录。
界面风格类似 DeepSeek（简洁、现代），但将对话区域改为问卷调查式表单（两个文本输入框 + 图片上传框）。

二、技术栈建议
层级	技术选择（推荐）	说明
前端	React / Vue 3 + TypeScript	构建现代化、响应式的单页应用，支持路由、状态管理
后端	FastAPI / Django + DRF	FastAPI 性能高，自动生成 OpenAPI 文档，适合机器学习集成；Django 功能完备，ORM 强大
数据库	PostgreSQL + Redis（可选）	PostgreSQL 存储用户与预测记录，Redis 可用于缓存或任务队列
模型服务	直接集成在后端 API 进程中	模型与特征提取代码以 Python 库形式加载，节省部署成本
文件存储	本地文件系统 / 云存储（AWS S3、阿里云 OSS）	存储上传的图片和生成的 SHAP 图表
身份认证	JWT (Access + Refresh Token)	无状态认证，适合前后端分离
部署	Docker + Nginx + Gunicorn/Uvicorn	容器化部署，方便扩展

三、数据库表结构设计
1. 用户表 users
字段名	类型	说明
id	integer (PK)	自增主键
username	varchar(50)	唯一，登录用户名
email	varchar(100)	可选
password_hash	varchar(128)	bcrypt 加密
created_at	timestamp	注册时间
2. 预测记录表 predictions
字段名	类型	说明
id	integer (PK)	自增主键
user_id	integer (FK)	关联 users.id
product_name	varchar(200)	用户输入的产品名称
product_desc	text	用户输入的产品描述
image_path	varchar(500)	上传图片的存储路径（相对或绝对）
features_snapshot	jsonb	存储提取的特征向量
predicted_sales	integer	预测的销量
repeat_rate	float	预测的复购率
average_rating	float	预测的平均评分
shap_plot_path	varchar(500)	生成的 SHAP 图表存储路径
created_at	timestamp	预测创建时间（历史记录按此排序）
注意：
features_snapshot 用 JSONB 类型可方便存储不定长的特征数组，方便后续分析。
image_path 和 shap_plot_path 存储相对路径，前端可通过静态文件路由访问。

四、后端 API 设计
基础信息
所有接口（除登录注册外）需携带 JWT Token（Header: Authorization: Bearer <token>）。

返回统一格式：{"code": 0, "msg": "success", "data": ...}。

1. 认证模块
方法	路径	描述
POST	/api/auth/register	用户注册（username, password, email）
POST	/api/auth/login	登录，返回 access_token, refresh_token
POST	/api/auth/refresh	刷新 access_token
2. 预测模块
方法	路径	描述
POST	/api/predict	提交表单（multipart/form-data），包含 product_name, product_desc, image_file。后端处理特征提取→模型预测→SHAP生成，返回预测值及 SHAP 图表 URL。同时将记录存入数据库。
GET	/api/predict/{id}	获取某次预测的详细信息（包括图片、SHAP图等）
3. 历史记录模块
方法	路径	描述
GET	/api/history	分页返回当前用户的所有预测记录，按时间倒序
GET	/api/history/{id}	同 /api/predict/{id} 可复用
DELETE	/api/history/{id}	删除某条历史记录（可选，需删除关联的图片文件）
五、前端界面设计
布局：类似 DeepSeek 的现代风格，深色/浅色主题，左侧可能为侧边栏（历史记录列表），右侧主区域为表单与结果展示。

登录/注册页：独立页面，使用 JWT 存储登录状态。

主界面：

表单区：两个输入框（产品名称、产品描述） + 图片上传组件（支持拖拽/点击，预览）。
提交按钮：点击后显示加载状态，调用 /api/predict。
结果展示区：显示预测销售值（数字加单位），并展示 SHAP 可解释性图表（图片）。
历史记录侧边栏：展示用户过往预测的摘要（产品名、预测值、时间），点击可加载详情（图片和 SHAP 图回显）。
路由：/（主页面，需登录），/login，/register。

六、模型集成与特征提取流程
参考src\shap_association_explain.py的代码逻辑，仅对输入输出做适配


七、历史记录与文件存储策略
用户上传的图片和生成的 SHAP 图应存储在同一个用户隔离的目录下，例如：
uploads/{user_id}/{timestamp}_{filename}.png
数据库中仅存储相对路径（相对于静态文件根目录）。
后端提供静态文件路由，使前端能直接访问这些图片。
删除记录时，需同时删除磁盘上的关联文件，避免存储膨胀。

八、补充建议与优化
1. 异步处理
如果模型推理或特征提取耗时较长（>5秒），可改用异步任务队列（如 Celery + Redis）避免请求阻塞，前端轮询结果。但作为 MVP，同步方式也可接受。
2. 输入校验与安全
限制图片大小（如 5MB），类型只允许 JPEG/PNG。
对文本输入做 XSS 过滤（前端转义，后端也可过滤）。
使用 python-multipart 处理文件上传。

3. 模型更新与版本管理
若模型需迭代，可设计模型版本表，或通过环境变量指定模型文件路径，方便热更新。

4. 前端优化
使用 React Query 或 Vue Query 管理服务器状态，方便处理缓存和重试。
历史记录采用无限滚动或分页加载，提升性能。

5. 可解释性增强
除了 SHAP 图表，可额外返回特征名称和对应贡献值，前端可自定义展示。
